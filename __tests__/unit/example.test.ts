/**
 * Example Unit Test
 *
 * Demonstrates the testing pattern for DriveLegal.
 * - Unit tests go in __tests__/unit/
 * - Integration tests go in __tests__/integration/
 * - Test utilities go in __tests__/utils/
 * - Mock data goes in __tests__/mocks/
 *
 * Run with: npm test
 */

describe('Example test suite', () => {
  it('should pass a basic assertion', () => {
    expect(1 + 1).toBe(2);
  });
});
