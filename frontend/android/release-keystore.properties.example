# Example configuration for production release signing.
# Rename this file to 'release-keystore.properties' and fill in your production values.
# DO NOT commit the actual release-keystore.properties file or your keystore file.

storeFile=my-release-key.keystore
storePassword=keystore_password
keyAlias=my-key-alias
keyPassword=key_password
